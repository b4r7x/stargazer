{
  "name": "@diffgazer/add",
  "version": "0.2.1",
  "description": "Diffgazer registry installer for UI components and keyboard hooks",
  "license": "MIT",
  "author": "diffgazer",
  "type": "module",
  "engines": {
    "node": ">=22.0.0"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/b4r7x/diffgazer.git",
    "directory": "cli/add"
  },
  "bugs": {
    "url": "https://github.com/b4r7x/diffgazer/issues"
  },
  "homepage": "https://github.com/b4r7x/diffgazer/tree/main/cli/add",
  "publishConfig": {
    "access": "public",
    "provenance": true
  },
  "keywords": [
    "cli",
    "ui",
    "components",
    "hooks",
    "react",
    "terminal",
    "diffgazer",
    "tailwind",
    "tailwindcss",
    "component-library"
  ],
  "bin": {
    "dgadd": "./dist/index.js"
  },
  "files": [
    "dist",
    "README.md",
    "CHANGELOG.md",
    "LICENSE",
    "SECURITY.md",
    "SUPPORT.md"
  ],
  "scripts": {
    "generate:bundles": "node --import tsx scripts/bundle-registry.ts && node --import tsx scripts/generate-keys-copy-bundle.ts",
    "generate:schema": "node --import tsx scripts/generate-config-schema.ts",
    "build": "if [ \"$DIFFGAZER_SKIP_ARTIFACT_PREPARE\" != \"1\" ]; then pnpm run generate:bundles; fi && rm -rf dist && tsup && node --import tsx scripts/copy-generated.ts",
    "validate:artifacts": "pnpm --dir ../.. validate:artifacts",
    "prepack": "pnpm run build",
    "dev": "tsc --noEmit --watch",
    "type-check": "tsc --noEmit && tsc --noEmit -p scripts/tsconfig.json && tsc --noEmit -p tsconfig.config.json",
    "check": "biome check .",
    "test": "vitest run",
    "test:types": "tsc --noEmit -p tsconfig.test.json",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@clack/prompts": "^0.11.0",
    "commander": "^13.0.0",
    "diff": "^8.0.3",
    "figlet": "^1.10.0",
    "picocolors": "^1.1.0",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@diffgazer/keys": "workspace:*",
    "@diffgazer/registry": "workspace:*",
    "@diffgazer/ui": "workspace:*",
    "@types/node": "^24.0.0",
    "tsup": "^8.5.1",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3",
    "vitest": "^4.1.0"
  }
}
